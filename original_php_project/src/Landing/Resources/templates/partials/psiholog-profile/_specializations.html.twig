<div class="card mt-2 shadow-sm">
    <div class="card-body">
        <h5 class="card-title">
            {{ 'Мои специализации'|trans }}
        </h5>
        <div class="card-text profile__specialization-container">
            {% for index, title in specializations %}
                <div class="landing-psiholog-specialization">
                    {{ title|trans }}
                </div>
            {% endfor %}
        </div>
    </div>
</div>